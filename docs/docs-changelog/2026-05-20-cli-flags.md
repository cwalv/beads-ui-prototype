# 2026-05-20 — bd-commands flag additions

## Scope

Iteration 7. Three small additions to `bd-commands.md` that
explanation-sweep deferred.

## Changes applied

### `bd init --remote` (GH#3527)

The `bd init` row in the Setup table now notes the `--remote <url>`
bootstrap flag — clones from an existing remote into a fresh local
DB. Cited the GH issue.

### `bd dolt push/pull --remote` (GH#3211)

The `bd dolt <sub>` row in the Sync table now notes that `push` and
`pull` accept `--remote <name>` to target a non-default remote.
Cited the GH issue.

### `bd dep` row refresh

Expanded the `bd dep` row in the Dependencies section:

- Listed full subcommand set including `list`; noted `rm` is an
  alias for `remove`.
- Documented GH#3565 `tree` rendering: dep-type per node + root-only
  `[BLOCKED]` badge gated by `IsBlockingEdge` predicate.

## Tips

- beads @ da73b7511ccac8069a53fcb7a8963e8c9c9433a6
- gascity @ 2c27373a92f9568f40935f5c5067bd33b8584ba6

## Gaps deferred to a future run

- **`json-outputs.md` per-command examples** still in legacy shape —
  only matters once `BD_JSON_ENVELOPE=1` becomes default in v2.0.
- **Tutorials and how-to pages** not yet audited in this sweep.

## See also

- `handbook/reference/bd-commands.md` § Setup, Sync, Dependencies
